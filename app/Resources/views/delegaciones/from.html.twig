{% extends 'navegacion.html.twig' %}

{% block formularios %}

    <h1 class="mb-5 bg-navegacion-cabecera">
        {% if delegacion.id %}
            Delegación {{ delegacion.nombre }}
        {% else %}
            Alta Delegación
        {% endif %}
    </h1>
<div class="bg-cuadrosNavegacion p-5">
    <a href="{{ path('delegacion_portada') }}" class="btn bg-info2 mt-5 mb-5"><i class="fas fa-arrow-alt-circle-left"></i> Volver atrás</a>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn bg-success2 mt-5"><i class="fas fa-save"></i> Guardar</button>
    {% if not delegacion.id %}
        <button type="reset" class="btn bg-warning2 mt-5"><i class="fas fa-broom"></i> Limpiar</button>
    {% else %}
        <a href="{{ path('delegacion_eliminar',{'id': delegacion.id}) }}" class="btn bg-danger2 mt-5"><i class="fas fa-trash"></i> Dar de baja</a>
    {% endif %}
    <a href="{{ path('delegaciones_listar') }}" class="btn bg-info2 mt-5"><i class="fas fa-arrow-alt-circle-left"></i> Ir al listado </a>

    {{ form_end(form) }}
</div>
{% endblock %}

{% block title %}
    Delegación
{% endblock %}

