{% extends 'navegacion.html.twig' %}

{% block formularios %}

    <h1>
        {% if albaran.id %}
            Albarán {{ albaran.id }}
        {% else %}
            Alta albarán
        {% endif %}
    </h1>
    <a href="{{ path('albaran_portada') }}" class="btn btn-info mt-5 mb-5"><i class="fas fa-arrow-alt-circle-left"></i> Volver atrás</a>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-success mt-5"><i class="fas fa-save"></i> Guardar</button>
    {% if not albaran.id %}
        <button type="reset" class="btn btn-warning mt-5"><i class="fas fa-broom "></i> Limpiar</button>
    {% else %}
        <a href="{{ path('albaranes_eliminar',{'id': albaran.id}) }}" class="btn btn-danger mt-5"><i class="fas fa-trash"></i> Borrar</a>
    {% endif %}
    <a href="{{ path('albaranes_Listar') }}" class="btn btn-info mt-5"><i class="fas fa-arrow-alt-circle-left"></i> Ir al listado </a>
    {% if albaran.id %}
        <a href="{{ path('contenido_Listar',{'id': albaran.id}) }}" class="btn btn-secondary mt-5"><i class="fas fa-clipboard"></i> Contenido</a>
    {% endif %}
    {{ form_end(form) }}
{% endblock %}

{% block title %}
    Albaranes
{% endblock %}

